Branding placeholder strategy
============================

The public home currently uses a local `BrandMark` React component as a temporary logo placeholder.
When final logo files are available, place them in this folder and replace the component internals without changing page layout contracts.
